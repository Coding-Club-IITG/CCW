# Future Enhancements & Features to Re-Implement

- **Spectator Mode**: Re-implement spectator functionality for Match Rooms (Arena & Blitz). Allow users to join a match room via a `?spectate=true` parameter. The UI should display a "Spectating" badge, hide the "Sync Submission" button, and replace the Ready button with a message like "Waiting for players to be ready...".
